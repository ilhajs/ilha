diff --git a/dist/index.js b/dist/index.js
index 15ba128..996f29b 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -1,6 +1,6 @@
 import { n as cn, t as toAttrs } from "./input-3v9lVkrk.js";
 import { n as config, r as handler, t as areiaPlugin } from "./plugin-BGD7GD6m.js";
-import { html, ilha, raw, untrack } from "ilha";
+import { html, ilha, raw, untrack, action, effect } from "ilha";
 import { jsx } from "ilha/jsx-runtime";
 import { Accordion, Checkbox as Checkbox$1, Collapsible as Collapsible$1, Combobox as Combobox$1, ContextMenu as ContextMenu$1, Dialog as Dialog$1, DropdownMenu, Field as Field$1, HoverCard as HoverCard$1, Popover as Popover$1, Progress as Progress$1, Resizable as Resizable$1, Slider as Slider$1, Switch as Switch$1, Tabs as Tabs$1, Toggle as Toggle$1, ToggleGroup as ToggleGroup$1, Tooltip as Tooltip$1 } from "@areia/slots";
 import { addMonths, eachDayOfInterval, endOfMonth, endOfWeek, format, isAfter, isBefore, isSameDay, isSameMonth, isWithinInterval, parseISO, startOfMonth, startOfWeek } from "date-fns";
@@ -1370,32 +1370,44 @@ function CheckboxGroup(inputOrChildren = {}, children) {
     ${description == null ? "" : html`<p class="text-sm text-areia-subtle">${description}</p>`}
   </fieldset>`;
 }
-const CheckboxRoot = ilha.input().action("checkedChange", (checked, { host }) => {
+const CheckboxRoot = ilha((input) => {
+    let host;
+    const checkedChange = action((checked) => {
 	getBindBridge(host, "checked")?.onUserChange(checked);
-}).onMount(({ host, input, action }) => {
-	const root = resolveCheckboxRoot(host);
-	if (!root) return;
-	disposeBindBridge(host, "checked");
-	const itemValue = typeof input.value === "string" ? input.value : void 0;
-	stampMorphPreserve(root);
-	const controller = Checkbox$1.createCheckbox(root, {
-		defaultChecked: typeof input.checked === "boolean" ? input.checked : void 0,
-		indeterminate: typeof input.indeterminate === "boolean" ? input.indeterminate : void 0,
-		disabled: typeof input.disabled === "boolean" ? input.disabled : void 0,
-		required: typeof input.required === "boolean" ? input.required : void 0,
-		name: typeof input.name === "string" ? input.name : void 0,
-		value: itemValue,
-		onCheckedChange: (checked) => action.checkedChange(checked)
-	});
-	createBindBridge(host, "checked", checkedBindSource(input, controller, {
-		itemValue,
-		onUserChange: (checked) => input.onCheckedChange?.(checked),
-		destroy: () => controller.destroy()
-	}));
-	return () => disposeBindBridge(host);
-}).effect(({ host }) => {
-	getBindBridge(host, "checked")?.applyFromSignal();
-}).render(({ input }) => renderCheckbox(input));
+});
+
+    effect.once(({ host: __host, signal }) => {
+        host = __host;
+
+        	const root = resolveCheckboxRoot(host);
+        	if (!root) return;
+        	disposeBindBridge(host, "checked");
+        	const itemValue = typeof input.value === "string" ? input.value : void 0;
+        	stampMorphPreserve(root);
+        	const controller = Checkbox$1.createCheckbox(root, {
+        		defaultChecked: typeof input.checked === "boolean" ? input.checked : void 0,
+        		indeterminate: typeof input.indeterminate === "boolean" ? input.indeterminate : void 0,
+        		disabled: typeof input.disabled === "boolean" ? input.disabled : void 0,
+        		required: typeof input.required === "boolean" ? input.required : void 0,
+        		name: typeof input.name === "string" ? input.name : void 0,
+        		value: itemValue,
+        		onCheckedChange: (checked) => checkedChange(checked)
+        	});
+        	createBindBridge(host, "checked", checkedBindSource(input, controller, {
+        		itemValue,
+        		onUserChange: (checked) => input.onCheckedChange?.(checked),
+        		destroy: () => controller.destroy()
+        	}));
+        	return () => disposeBindBridge(host);
+    });
+
+    effect(() => {
+
+        	getBindBridge(host, "checked")?.applyFromSignal();
+    });
+
+    return renderCheckbox(input);
+});
 function resolveCheckboxRoot(host) {
 	return host.matches("[data-slot=\"checkbox\"]") ? host : host.querySelector("[data-slot=\"checkbox\"]");
 }
@@ -1519,17 +1531,25 @@ function bindFieldRoot(root, options = {}) {
 function FieldBase(input = {}) {
 	return renderField$1(input);
 }
-const FieldRoot = ilha.input().onMount(({ host, input }) => {
-	const root = host.matches("[data-slot=\"field\"]") ? host : host.querySelector("[data-slot=\"field\"]");
-	if (!root) return;
-	return bindFieldRoot(root, {
-		name: input.name,
-		disabled: input.disabled,
-		invalid: input.invalid,
-		validate: input.validate,
-		validationMode: input.validationMode
-	});
-}).render(({ input }) => renderField$1(input));
+const FieldRoot = ilha((input) => {
+    let host;
+
+    effect.once(({ host: __host, signal }) => {
+        host = __host;
+
+        	const root = host.matches("[data-slot=\"field\"]") ? host : host.querySelector("[data-slot=\"field\"]");
+        	if (!root) return;
+        	return bindFieldRoot(root, {
+        		name: input.name,
+        		disabled: input.disabled,
+        		invalid: input.invalid,
+        		validate: input.validate,
+        		validationMode: input.validationMode
+        	});
+    });
+
+    return renderField$1(input);
+});
 const Field = Object.assign(FieldRoot, {
 	Root: FieldRoot,
 	Static: FieldBase,
@@ -1782,27 +1802,35 @@ function bindTooltipRoot(root, options = {}) {
 		tooltipControllers.delete(root);
 	};
 }
-const TooltipRoot = ilha.input().onMount(({ host, input }) => {
-	const root = host.matches("[data-slot=\"tooltip\"]") ? host : host.querySelector("[data-slot=\"tooltip\"]");
-	if (!root) return;
-	return bindTooltipRoot(root, {
-		align: input.align,
-		alignOffset: input.alignOffset,
-		avoidCollisions: input.avoidCollisions,
-		collisionPadding: input.collisionPadding,
-		delay: input.delay,
-		onOpenChange: input.onOpenChange,
-		portal: input.portal,
-		side: input.side,
-		sideOffset: input.sideOffset,
-		skipDelayDuration: input.skipDelayDuration,
-		onPortalMounted: input.onPortalMounted
-	});
-}).render(({ input }) => renderTooltip(normalizeStaticChildSlots(input, [
+const TooltipRoot = ilha((input) => {
+    let host;
+
+    effect.once(({ host: __host, signal }) => {
+        host = __host;
+
+        	const root = host.matches("[data-slot=\"tooltip\"]") ? host : host.querySelector("[data-slot=\"tooltip\"]");
+        	if (!root) return;
+        	return bindTooltipRoot(root, {
+        		align: input.align,
+        		alignOffset: input.alignOffset,
+        		avoidCollisions: input.avoidCollisions,
+        		collisionPadding: input.collisionPadding,
+        		delay: input.delay,
+        		onOpenChange: input.onOpenChange,
+        		portal: input.portal,
+        		side: input.side,
+        		sideOffset: input.sideOffset,
+        		skipDelayDuration: input.skipDelayDuration,
+        		onPortalMounted: input.onPortalMounted
+        	});
+    });
+
+    return renderTooltip(normalizeStaticChildSlots(input, [
 	"content",
 	"trigger",
 	"children"
-])));
+]));
+});
 function TooltipBase(input) {
 	return renderTooltip(normalizeStaticChildSlots(input, [
 		"content",
@@ -1939,7 +1967,9 @@ const clipboardTimers = /* @__PURE__ */ new WeakMap();
 function resolveClipboardRoot(host) {
 	return host.matches("[data-slot=\"clipboard-text\"]") ? host : host.querySelector("[data-slot=\"clipboard-text\"]");
 }
-const ClipboardTextRoot = ilha.input().action("copy", async (_props, { input, host, signal }) => {
+const ClipboardTextRoot = ilha((input) => {
+    let host;
+    const copy = action(async (_props, { signal }) => {
 	const root = resolveClipboardRoot(host);
 	if (!root) return;
 	const text = input.textToCopy ?? input.text;
@@ -1956,10 +1986,22 @@ const ClipboardTextRoot = ilha.input().action("copy", async (_props, { input, ho
 		if (signal.aborted) return;
 		console.warn("Clipboard copy failed", error);
 	}
-}).on("[data-slot='clipboard-text-button']@click", ({ action, input }) => {
-	if (input.onCopy == null) return;
-	action.copy();
-}).render(({ input }) => renderClipboardText(input));
+});
+
+    effect.once(({ host: __host }) => { host = __host; });
+
+    effect.once(({ host, signal }) => {
+        host.addEventListener("click", (event) => {
+            const __target = event.target;
+            if (!(__target instanceof Element) || !__target.closest("[data-slot='clipboard-text-button']")) return;
+
+            	if (input.onCopy == null) return;
+            	copy();
+        }, { signal });
+    });
+
+    return renderClipboardText(input);
+});
 function ClipboardTextBase(input) {
 	return renderClipboardText(input);
 }
@@ -2113,40 +2155,52 @@ function renderCollapsible(input = {}) {
 		children: children ?? [CollapsibleDefaultTrigger({ children: trigger ?? "Toggle" }), CollapsibleDefaultPanel({ children: panel })]
 	});
 }
-const CollapsibleRootIsland = ilha.input().action("openChange", (open, { host }) => {
+const CollapsibleRootIsland = ilha((input) => {
+    let host;
+    const openChange = action((open) => {
 	getBindBridge(host, "open")?.onUserChange(open);
-}).onMount(({ host, input, action }) => {
-	const accordionRoot = host.matches("[data-slot=\"accordion\"]") ? host : host.querySelector("[data-slot=\"accordion\"]");
-	if (accordionRoot) {
-		stampMorphPreserve(accordionRoot, MORPH_CONTROLLER_STYLE);
-		const controller = Accordion.createAccordion(accordionRoot, {
-			multiple: input.multiple,
-			defaultValue: input.defaultValue ?? input.value,
-			disabled: input.disabled,
-			orientation: input.orientation,
-			loopFocus: input.loopFocus,
-			hiddenUntilFound: input.hiddenUntilFound,
-			onValueChange: input.onValueChange
-		});
-		return () => controller.destroy();
-	}
-	const root = host.matches("[data-slot=\"collapsible\"]") ? host : host.querySelector("[data-slot=\"collapsible\"]");
-	if (!root) return;
-	disposeBindBridge(host, "open");
-	stampMorphPreserve(root, MORPH_CONTROLLER_STYLE);
-	const controller = Collapsible$1.createCollapsible(root, {
-		defaultOpen: openBindDefault(input, input.defaultOpen ?? input.open),
-		hiddenUntilFound: input.hiddenUntilFound,
-		onOpenChange: (open) => action.openChange(open)
-	});
-	createBindBridge(host, "open", openBindSource(input, controller, {
-		onUserChange: (open) => input.onOpenChange?.(open),
-		destroy: () => controller.destroy()
-	}));
-	return () => disposeBindBridge(host);
-}).effect(({ host }) => {
-	getBindBridge(host, "open")?.applyFromSignal();
-}).render(({ input }) => renderCollapsible(input));
+});
+
+    effect.once(({ host: __host, signal }) => {
+        host = __host;
+
+        	const accordionRoot = host.matches("[data-slot=\"accordion\"]") ? host : host.querySelector("[data-slot=\"accordion\"]");
+        	if (accordionRoot) {
+        		stampMorphPreserve(accordionRoot, MORPH_CONTROLLER_STYLE);
+        		const controller = Accordion.createAccordion(accordionRoot, {
+        			multiple: input.multiple,
+        			defaultValue: input.defaultValue ?? input.value,
+        			disabled: input.disabled,
+        			orientation: input.orientation,
+        			loopFocus: input.loopFocus,
+        			hiddenUntilFound: input.hiddenUntilFound,
+        			onValueChange: input.onValueChange
+        		});
+        		return () => controller.destroy();
+        	}
+        	const root = host.matches("[data-slot=\"collapsible\"]") ? host : host.querySelector("[data-slot=\"collapsible\"]");
+        	if (!root) return;
+        	disposeBindBridge(host, "open");
+        	stampMorphPreserve(root, MORPH_CONTROLLER_STYLE);
+        	const controller = Collapsible$1.createCollapsible(root, {
+        		defaultOpen: openBindDefault(input, input.defaultOpen ?? input.open),
+        		hiddenUntilFound: input.hiddenUntilFound,
+        		onOpenChange: (open) => openChange(open)
+        	});
+        	createBindBridge(host, "open", openBindSource(input, controller, {
+        		onUserChange: (open) => input.onOpenChange?.(open),
+        		destroy: () => controller.destroy()
+        	}));
+        	return () => disposeBindBridge(host);
+    });
+
+    effect(() => {
+
+        	getBindBridge(host, "open")?.applyFromSignal();
+    });
+
+    return renderCollapsible(input);
+});
 const Collapsible = Object.assign(CollapsibleRootIsland, {
 	Root: CollapsibleRootIsland,
 	RootIsland: CollapsibleRootIsland,
@@ -2231,28 +2285,40 @@ function renderContextMenu(input = {}) {
 		"data-close-on-select": closeOnSelect
 	})}`, inner);
 }
-const ContextMenuRoot = ilha.input().action("openChange", (open, { host }) => {
+const ContextMenuRoot = ilha((input) => {
+    let host;
+    const openChange = action((open) => {
 	getBindBridge(host, "open")?.onUserChange(open);
-}).onMount(({ host, input, action }) => {
-	const root = host.matches("[data-slot=\"context-menu\"]") ? host : host.querySelector("[data-slot=\"context-menu\"]");
-	if (!root) return;
-	disposeBindBridge(host, "open");
-	stampMorphPreserve(root, MORPH_CONTROLLER_STYLE);
-	const controller = ContextMenu$1.createContextMenu(root, {
-		disabled: input.disabled,
-		closeOnSelect: input.closeOnSelect,
-		onOpenChange: (open) => action.openChange(open),
-		onSelect: input.onSelect,
-		onPortalMounted: input.onPortalMounted
-	});
-	createBindBridge(host, "open", openBindSource(input, controller, {
-		onUserChange: (open) => input.onOpenChange?.(open),
-		destroy: () => controller.destroy()
-	}));
-	return () => disposeBindBridge(host);
-}).effect(({ host }) => {
-	getBindBridge(host, "open")?.applyFromSignal();
-}).render(({ input }) => renderContextMenu(input));
+});
+
+    effect.once(({ host: __host, signal }) => {
+        host = __host;
+
+        	const root = host.matches("[data-slot=\"context-menu\"]") ? host : host.querySelector("[data-slot=\"context-menu\"]");
+        	if (!root) return;
+        	disposeBindBridge(host, "open");
+        	stampMorphPreserve(root, MORPH_CONTROLLER_STYLE);
+        	const controller = ContextMenu$1.createContextMenu(root, {
+        		disabled: input.disabled,
+        		closeOnSelect: input.closeOnSelect,
+        		onOpenChange: (open) => openChange(open),
+        		onSelect: input.onSelect,
+        		onPortalMounted: input.onPortalMounted
+        	});
+        	createBindBridge(host, "open", openBindSource(input, controller, {
+        		onUserChange: (open) => input.onOpenChange?.(open),
+        		destroy: () => controller.destroy()
+        	}));
+        	return () => disposeBindBridge(host);
+    });
+
+    effect(() => {
+
+        	getBindBridge(host, "open")?.applyFromSignal();
+    });
+
+    return renderContextMenu(input);
+});
 const ContextMenu = Object.assign(ContextMenuRoot, {
 	Root: ContextMenuRoot,
 	Static: renderContextMenu,
@@ -2486,78 +2552,90 @@ function syncRoot(root, input) {
 	for (const { name } of Array.from(root.attributes)) if (name.startsWith("data-") && !next.hasAttribute(name)) root.removeAttribute(name);
 	for (const { name, value } of Array.from(next.attributes)) if (name.startsWith("data-")) root.setAttribute(name, value);
 }
-const DatePickerRoot = ilha.input().action("change", (selected, { host }) => {
+const DatePickerRoot = ilha((input) => {
+    let host;
+    const change = action((selected) => {
 	getBindBridge(host, "date")?.onUserChange(selected);
-}).onMount(({ host, input, action }) => {
-	const root = host.matches("[data-slot=\"date-picker\"]") ? host : host.querySelector("[data-slot=\"date-picker\"]");
-	if (!root) return;
-	stampMorphPreserve(root);
-	disposeBindBridge(host, "date");
-	let currentInput = {
-		...input,
-		month: parseDate(root.dataset["month"]) ?? resolveInitialMonth(input),
-		selected: getBindAccessor(input, "bind:valueAsDate")?.() ?? selectedFromDataset(root, input.mode ?? DATE_PICKER_DEFAULT_VARIANTS.mode)
-	};
-	const getSelectedDate = () => {
-		const selected = currentInput.selected;
-		if (selected instanceof Date) return selected;
-		if (selected == null) return null;
-	};
-	const setSelectedDate = (date) => {
-		currentInput = {
-			...currentInput,
-			selected: date ?? void 0
-		};
-		syncRoot(root, currentInput);
-		emitChange(root, currentInput.selected);
-	};
-	createBindBridge(host, "date", dateBindSource(input, {
-		getDate: getSelectedDate,
-		setDate: setSelectedDate
-	}, { onUserChange: (date) => input.onChange?.(date ?? void 0) }));
-	const cleanupThis = applyThisBind(root, input);
-	const handleClick = (event) => {
-		const target = event.target;
-		if (!target) return;
-		const currentMonth = currentInput.month ?? resolveInitialMonth(currentInput);
-		const previous = target.closest("[data-date-picker-prev]");
-		const next = target.closest("[data-date-picker-next]");
-		if (previous || next) {
-			const month = addMonths(currentMonth, previous ? -1 : 1);
-			currentInput = {
-				...currentInput,
-				month
-			};
-			syncRoot(root, currentInput);
-			input.onMonthChange?.(month);
-			emitMonthChange(root, month);
-			return;
-		}
-		const day = target.closest("[data-date-picker-day]");
-		const date = parseDate(day?.dataset["date"]);
-		if (!day || !date || day.hasAttribute("disabled")) return;
-		if ((currentInput.mode ?? DATE_PICKER_DEFAULT_VARIANTS.mode) === "single" && input["bind:valueAsDate"]) {
-			action.change(date);
-			return;
-		}
-		const selected = nextSelected(currentInput.selected, date, currentInput);
-		currentInput = {
-			...currentInput,
-			selected
-		};
-		syncRoot(root, currentInput);
-		action.change(selected);
-		emitChange(root, selected);
-	};
-	root.addEventListener("click", handleClick);
-	return () => {
-		cleanupThis?.();
-		root.removeEventListener("click", handleClick);
-		disposeBindBridge(host);
-	};
-}).effect(({ host }) => {
-	getBindBridge(host, "date")?.applyFromSignal();
-}).render(({ input }) => renderDatePicker(input));
+});
+
+    effect.once(({ host: __host, signal }) => {
+        host = __host;
+
+        	const root = host.matches("[data-slot=\"date-picker\"]") ? host : host.querySelector("[data-slot=\"date-picker\"]");
+        	if (!root) return;
+        	stampMorphPreserve(root);
+        	disposeBindBridge(host, "date");
+        	let currentInput = {
+        		...input,
+        		month: parseDate(root.dataset["month"]) ?? resolveInitialMonth(input),
+        		selected: getBindAccessor(input, "bind:valueAsDate")?.() ?? selectedFromDataset(root, input.mode ?? DATE_PICKER_DEFAULT_VARIANTS.mode)
+        	};
+        	const getSelectedDate = () => {
+        		const selected = currentInput.selected;
+        		if (selected instanceof Date) return selected;
+        		if (selected == null) return null;
+        	};
+        	const setSelectedDate = (date) => {
+        		currentInput = {
+        			...currentInput,
+        			selected: date ?? void 0
+        		};
+        		syncRoot(root, currentInput);
+        		emitChange(root, currentInput.selected);
+        	};
+        	createBindBridge(host, "date", dateBindSource(input, {
+        		getDate: getSelectedDate,
+        		setDate: setSelectedDate
+        	}, { onUserChange: (date) => input.onChange?.(date ?? void 0) }));
+        	const cleanupThis = applyThisBind(root, input);
+        	const handleClick = (event) => {
+        		const target = event.target;
+        		if (!target) return;
+        		const currentMonth = currentInput.month ?? resolveInitialMonth(currentInput);
+        		const previous = target.closest("[data-date-picker-prev]");
+        		const next = target.closest("[data-date-picker-next]");
+        		if (previous || next) {
+        			const month = addMonths(currentMonth, previous ? -1 : 1);
+        			currentInput = {
+        				...currentInput,
+        				month
+        			};
+        			syncRoot(root, currentInput);
+        			input.onMonthChange?.(month);
+        			emitMonthChange(root, month);
+        			return;
+        		}
+        		const day = target.closest("[data-date-picker-day]");
+        		const date = parseDate(day?.dataset["date"]);
+        		if (!day || !date || day.hasAttribute("disabled")) return;
+        		if ((currentInput.mode ?? DATE_PICKER_DEFAULT_VARIANTS.mode) === "single" && input["bind:valueAsDate"]) {
+        			change(date);
+        			return;
+        		}
+        		const selected = nextSelected(currentInput.selected, date, currentInput);
+        		currentInput = {
+        			...currentInput,
+        			selected
+        		};
+        		syncRoot(root, currentInput);
+        		change(selected);
+        		emitChange(root, selected);
+        	};
+        	root.addEventListener("click", handleClick);
+        	return () => {
+        		cleanupThis?.();
+        		root.removeEventListener("click", handleClick);
+        		disposeBindBridge(host);
+        	};
+    });
+
+    effect(() => {
+
+        	getBindBridge(host, "date")?.applyFromSignal();
+    });
+
+    return renderDatePicker(input);
+});
 function DatePickerBase(input = {}) {
 	return renderDatePicker(input);
 }
@@ -2774,35 +2852,47 @@ function renderDialog(input = {}) {
 		lockScroll
 	})}${toAttrs(rootProps)}`, inner);
 }
-const DialogRoot = ilha.input().action("openChange", (open, { host }) => {
+const DialogRoot = ilha((input) => {
+    let host;
+    const openChange = action((open) => {
 	getBindBridge(host, "open")?.onUserChange(open);
-}).onMount(({ host, input, action }) => {
-	const root = host.matches("[data-slot=\"dialog\"]") ? host : host.querySelector("[data-slot=\"dialog\"]");
-	if (!root) return;
-	const isAlertDialog = input.alertDialog ?? input.role === "alertdialog";
-	disposeBindBridge(host, "open");
-	stampMorphPreserve(root, MORPH_CONTROLLER_STYLE);
-	const controller = Dialog$1.createDialog(root, {
-		alertDialog: isAlertDialog,
-		closeOnClickOutside: input.closeOnClickOutside ?? (isAlertDialog ? false : void 0),
-		closeOnEscape: input.closeOnEscape,
-		defaultOpen: openBindDefault(input, input.defaultOpen),
-		lockScroll: input.lockScroll,
-		onOpenChange: (open) => action.openChange(open),
-		onPortalMounted: input.onPortalMounted
-	});
-	createBindBridge(host, "open", openBindSource(input, controller, {
-		onUserChange: (open) => input.onOpenChange?.(open),
-		destroy: () => controller.destroy()
-	}));
-	return () => disposeBindBridge(host);
-}).effect(({ host }) => {
-	getBindBridge(host, "open")?.applyFromSignal();
-}).render(({ input }) => renderDialog(normalizeStaticChildSlots(input, [
+});
+
+    effect.once(({ host: __host, signal }) => {
+        host = __host;
+
+        	const root = host.matches("[data-slot=\"dialog\"]") ? host : host.querySelector("[data-slot=\"dialog\"]");
+        	if (!root) return;
+        	const isAlertDialog = input.alertDialog ?? input.role === "alertdialog";
+        	disposeBindBridge(host, "open");
+        	stampMorphPreserve(root, MORPH_CONTROLLER_STYLE);
+        	const controller = Dialog$1.createDialog(root, {
+        		alertDialog: isAlertDialog,
+        		closeOnClickOutside: input.closeOnClickOutside ?? (isAlertDialog ? false : void 0),
+        		closeOnEscape: input.closeOnEscape,
+        		defaultOpen: openBindDefault(input, input.defaultOpen),
+        		lockScroll: input.lockScroll,
+        		onOpenChange: (open) => openChange(open),
+        		onPortalMounted: input.onPortalMounted
+        	});
+        	createBindBridge(host, "open", openBindSource(input, controller, {
+        		onUserChange: (open) => input.onOpenChange?.(open),
+        		destroy: () => controller.destroy()
+        	}));
+        	return () => disposeBindBridge(host);
+    });
+
+    effect(() => {
+
+        	getBindBridge(host, "open")?.applyFromSignal();
+    });
+
+    return renderDialog(normalizeStaticChildSlots(input, [
 	"content",
 	"trigger",
 	"children"
-])));
+]));
+});
 function DialogBase(input = {}) {
 	return renderDialog(normalizeStaticChildSlots(input, [
 		"content",
@@ -3003,42 +3093,54 @@ function renderDropdown(input = {}) {
 		"data-highlight-item-on-hover": highlightItemOnHover
 	})}`, inner);
 }
-const DropdownRoot = ilha.input().action("openChange", (open, { host }) => {
+const DropdownRoot = ilha((input) => {
+    let host;
+    const openChange = action((open) => {
 	getBindBridge(host, "open")?.onUserChange(open);
-}).onMount(({ host, input, action }) => {
-	const root = host.matches("[data-slot=\"dropdown-menu\"]") ? host : host.querySelector("[data-slot=\"dropdown-menu\"]");
-	if (!root) return;
-	disposeBindBridge(host, "open");
-	stampMorphPreserve(root, MORPH_CONTROLLER_STYLE);
-	const controller = DropdownMenu.createDropdownMenu(root, {
-		defaultOpen: openBindDefault(input, input.defaultOpen),
-		defaultValue: input.defaultValue,
-		defaultValues: input.defaultValues,
-		onOpenChange: (open) => action.openChange(open),
-		onSelect: input.onSelect,
-		onValueChange: input.onValueChange,
-		onValuesChange: input.onValuesChange,
-		closeOnClickOutside: input.closeOnClickOutside,
-		closeOnEscape: input.closeOnEscape,
-		closeOnSelect: input.closeOnSelect,
-		side: input.side,
-		align: input.align,
-		sideOffset: input.sideOffset,
-		alignOffset: input.alignOffset,
-		avoidCollisions: input.avoidCollisions,
-		collisionPadding: input.collisionPadding,
-		lockScroll: input.lockScroll,
-		highlightItemOnHover: input.highlightItemOnHover,
-		onPortalMounted: input.onPortalMounted
-	});
-	createBindBridge(host, "open", openBindSource(input, controller, {
-		onUserChange: (open) => input.onOpenChange?.(open),
-		destroy: () => controller.destroy()
-	}));
-	return () => disposeBindBridge(host);
-}).effect(({ host }) => {
-	getBindBridge(host, "open")?.applyFromSignal();
-}).render(({ input }) => renderDropdown(input));
+});
+
+    effect.once(({ host: __host, signal }) => {
+        host = __host;
+
+        	const root = host.matches("[data-slot=\"dropdown-menu\"]") ? host : host.querySelector("[data-slot=\"dropdown-menu\"]");
+        	if (!root) return;
+        	disposeBindBridge(host, "open");
+        	stampMorphPreserve(root, MORPH_CONTROLLER_STYLE);
+        	const controller = DropdownMenu.createDropdownMenu(root, {
+        		defaultOpen: openBindDefault(input, input.defaultOpen),
+        		defaultValue: input.defaultValue,
+        		defaultValues: input.defaultValues,
+        		onOpenChange: (open) => openChange(open),
+        		onSelect: input.onSelect,
+        		onValueChange: input.onValueChange,
+        		onValuesChange: input.onValuesChange,
+        		closeOnClickOutside: input.closeOnClickOutside,
+        		closeOnEscape: input.closeOnEscape,
+        		closeOnSelect: input.closeOnSelect,
+        		side: input.side,
+        		align: input.align,
+        		sideOffset: input.sideOffset,
+        		alignOffset: input.alignOffset,
+        		avoidCollisions: input.avoidCollisions,
+        		collisionPadding: input.collisionPadding,
+        		lockScroll: input.lockScroll,
+        		highlightItemOnHover: input.highlightItemOnHover,
+        		onPortalMounted: input.onPortalMounted
+        	});
+        	createBindBridge(host, "open", openBindSource(input, controller, {
+        		onUserChange: (open) => input.onOpenChange?.(open),
+        		destroy: () => controller.destroy()
+        	}));
+        	return () => disposeBindBridge(host);
+    });
+
+    effect(() => {
+
+        	getBindBridge(host, "open")?.applyFromSignal();
+    });
+
+    return renderDropdown(input);
+});
 const Dropdown = Object.assign(DropdownRoot, {
 	Root: DropdownRoot,
 	Static: renderDropdown,
@@ -3678,59 +3780,72 @@ function renderField(input, children) {
 		children: control
 	});
 }
-const ComboboxRoot = ilha.input().action("openChange", (open, { host }) => {
+const ComboboxRoot = ilha((input) => {
+    let host;
+    const openChange = action((open) => {
 	getBindBridge(host, "open")?.onUserChange(open);
-}).action("valueChange", (value, { host }) => {
+});
+    const valueChange = action((value) => {
 	getBindBridge(host, "value")?.onUserChange(value);
-}).onMount(({ host, input, action }) => {
-	const root = host.matches("[data-slot=\"combobox\"]") ? host : host.querySelector("[data-slot=\"combobox\"]");
-	if (!root) return;
-	disposeBindBridge(host);
-	const selectionInput = withSelectionBind(input);
-	stampMorphPreserve(root, MORPH_CONTROLLER_STYLE);
-	const controller = Combobox$1.createCombobox(root, {
-		align: input.align,
-		alignOffset: input.alignOffset,
-		autoHighlight: input.autoHighlight,
-		avoidCollisions: input.avoidCollisions,
-		collisionPadding: input.collisionPadding,
-		defaultOpen: openBindDefault(input, input.defaultOpen),
-		defaultValue: groupBindDefault(selectionInput, input.defaultValue) ?? void 0,
-		disabled: input.disabled,
-		filter: input.filter,
-		itemToStringValue: input.itemToStringValue,
-		multiple: input.multiple,
-		name: input.name,
-		onInputValueChange: input.onInputValueChange,
-		onOpenChange: (open) => action.openChange(open),
-		onValueChange: (value) => action.valueChange(value),
-		openOnFocus: input.openOnFocus,
-		placeholder: input.placeholder,
-		required: input.required,
-		side: input.side,
-		sideOffset: input.sideOffset,
-		onPortalMounted: input.onPortalMounted
-	});
-	createBindBridge(host, "open", openBindSource(input, controller, { onUserChange: (open) => input.onOpenChange?.(open) }));
-	createBindBridge(host, "value", groupBindSource(selectionInput, {
-		getValue: () => input.multiple ? [...controller.values] : controller.value,
-		setValue: (value) => {
-			if (value == null) controller.clear();
-			else controller.setValues(Array.isArray(value) ? value : [value]);
-		}
-	}, {
-		mode: input.multiple ? "multiple" : "single",
-		onUserChange: (value) => input.onValueChange?.(value)
-	}));
-	const cleanupThis = applyThisBind(root.querySelector("[data-slot=\"combobox-input\"]") ?? root.querySelector("[data-slot=\"combobox\"]"), input);
-	return () => {
-		cleanupThis?.();
-		disposeBindBridge(host);
-	};
-}).effect(({ host }) => {
-	getBindBridge(host, "open")?.applyFromSignal();
-	getBindBridge(host, "value")?.applyFromSignal();
-}).render(({ input }) => renderField(input));
+});
+
+    effect.once(({ host: __host, signal }) => {
+        host = __host;
+
+        	const root = host.matches("[data-slot=\"combobox\"]") ? host : host.querySelector("[data-slot=\"combobox\"]");
+        	if (!root) return;
+        	disposeBindBridge(host);
+        	const selectionInput = withSelectionBind(input);
+        	stampMorphPreserve(root, MORPH_CONTROLLER_STYLE);
+        	const controller = Combobox$1.createCombobox(root, {
+        		align: input.align,
+        		alignOffset: input.alignOffset,
+        		autoHighlight: input.autoHighlight,
+        		avoidCollisions: input.avoidCollisions,
+        		collisionPadding: input.collisionPadding,
+        		defaultOpen: openBindDefault(input, input.defaultOpen),
+        		defaultValue: groupBindDefault(selectionInput, input.defaultValue) ?? void 0,
+        		disabled: input.disabled,
+        		filter: input.filter,
+        		itemToStringValue: input.itemToStringValue,
+        		multiple: input.multiple,
+        		name: input.name,
+        		onInputValueChange: input.onInputValueChange,
+        		onOpenChange: (open) => openChange(open),
+        		onValueChange: (value) => valueChange(value),
+        		openOnFocus: input.openOnFocus,
+        		placeholder: input.placeholder,
+        		required: input.required,
+        		side: input.side,
+        		sideOffset: input.sideOffset,
+        		onPortalMounted: input.onPortalMounted
+        	});
+        	createBindBridge(host, "open", openBindSource(input, controller, { onUserChange: (open) => input.onOpenChange?.(open) }));
+        	createBindBridge(host, "value", groupBindSource(selectionInput, {
+        		getValue: () => input.multiple ? [...controller.values] : controller.value,
+        		setValue: (value) => {
+        			if (value == null) controller.clear();
+        			else controller.setValues(Array.isArray(value) ? value : [value]);
+        		}
+        	}, {
+        		mode: input.multiple ? "multiple" : "single",
+        		onUserChange: (value) => input.onValueChange?.(value)
+        	}));
+        	const cleanupThis = applyThisBind(root.querySelector("[data-slot=\"combobox-input\"]") ?? root.querySelector("[data-slot=\"combobox\"]"), input);
+        	return () => {
+        		cleanupThis?.();
+        		disposeBindBridge(host);
+        	};
+    });
+
+    effect(() => {
+
+        	getBindBridge(host, "open")?.applyFromSignal();
+        	getBindBridge(host, "value")?.applyFromSignal();
+    });
+
+    return renderField(input);
+});
 function ComboboxBase(inputOrChildren = {}, children) {
 	return renderField(Array.isArray(inputOrChildren) ? {} : inputOrChildren, Array.isArray(inputOrChildren) ? inputOrChildren : children);
 }
@@ -4042,61 +4157,87 @@ function syncPopoverArrowSide(root) {
 	const side = content?.getAttribute("data-side");
 	if (arrow && side) arrow.setAttribute("data-side", side);
 }
-const PopoverRoot = ilha.input().action("openChange", (open, { host }) => {
+const PopoverRoot = ilha((input) => {
+    let host;
+    const openChange = action((open) => {
 	getBindBridge(host, "open")?.onUserChange(open);
-}).onMount(({ host, input, action }) => {
-	const root = host.matches("[data-slot=\"popover\"]") ? host : host.querySelector("[data-slot=\"popover\"]");
-	if (!root) return;
-	const syncArrowSide = () => {
-		const content = root.querySelector("[data-slot=\"popover-content\"]");
-		const arrow = root.querySelector("[data-slot=\"popover-arrow\"]");
-		const side = content?.getAttribute("data-side");
-		if (arrow && side) arrow.setAttribute("data-side", side);
-	};
-	disposeBindBridge(host, "open");
-	stampMorphPreserve(root, MORPH_CONTROLLER_STYLE);
-	const controller = Popover$1.createPopover(root, {
-		align: input.align,
-		alignOffset: input.alignOffset,
-		avoidCollisions: input.avoidCollisions,
-		closeOnClickOutside: input.closeOnClickOutside,
-		closeOnEscape: input.closeOnEscape,
-		collisionPadding: input.collisionPadding,
-		defaultOpen: openBindDefault(input, input.defaultOpen),
-		onOpenChange: (open) => action.openChange(open),
-		portal: input.portal,
-		side: input.side,
-		sideOffset: input.sideOffset,
-		onPortalMounted: input.onPortalMounted
-	});
-	syncArrowSide();
-	const content = root.querySelector("[data-slot=\"popover-content\"]");
-	const observer = content ? new MutationObserver(syncArrowSide) : void 0;
-	observer?.observe(content, {
-		attributes: true,
-		attributeFilter: ["data-side"]
-	});
-	createBindBridge(host, "open", openBindSource(input, controller, {
-		onUserChange: (open) => input.onOpenChange?.(open),
-		destroy: () => {
-			observer?.disconnect();
-			controller.destroy();
-		}
-	}));
-	return () => disposeBindBridge(host);
-}).effect(({ host }) => {
-	getBindBridge(host, "open")?.applyFromSignal();
-}).on("[data-slot='popover-content']@animationend", ({ host }) => {
-	const root = host.matches("[data-slot=\"popover\"]") ? host : host.querySelector("[data-slot=\"popover\"]");
-	if (root) syncPopoverArrowSide(root);
-}).on("[data-slot='popover-content']@transitionend", ({ host }) => {
-	const root = host.matches("[data-slot=\"popover\"]") ? host : host.querySelector("[data-slot=\"popover\"]");
-	if (root) syncPopoverArrowSide(root);
-}).render(({ input }) => renderPopover(normalizeStaticChildSlots(input, [
+});
+
+    effect.once(({ host: __host, signal }) => {
+        host = __host;
+
+        	const root = host.matches("[data-slot=\"popover\"]") ? host : host.querySelector("[data-slot=\"popover\"]");
+        	if (!root) return;
+        	const syncArrowSide = () => {
+        		const content = root.querySelector("[data-slot=\"popover-content\"]");
+        		const arrow = root.querySelector("[data-slot=\"popover-arrow\"]");
+        		const side = content?.getAttribute("data-side");
+        		if (arrow && side) arrow.setAttribute("data-side", side);
+        	};
+        	disposeBindBridge(host, "open");
+        	stampMorphPreserve(root, MORPH_CONTROLLER_STYLE);
+        	const controller = Popover$1.createPopover(root, {
+        		align: input.align,
+        		alignOffset: input.alignOffset,
+        		avoidCollisions: input.avoidCollisions,
+        		closeOnClickOutside: input.closeOnClickOutside,
+        		closeOnEscape: input.closeOnEscape,
+        		collisionPadding: input.collisionPadding,
+        		defaultOpen: openBindDefault(input, input.defaultOpen),
+        		onOpenChange: (open) => openChange(open),
+        		portal: input.portal,
+        		side: input.side,
+        		sideOffset: input.sideOffset,
+        		onPortalMounted: input.onPortalMounted
+        	});
+        	syncArrowSide();
+        	const content = root.querySelector("[data-slot=\"popover-content\"]");
+        	const observer = content ? new MutationObserver(syncArrowSide) : void 0;
+        	observer?.observe(content, {
+        		attributes: true,
+        		attributeFilter: ["data-side"]
+        	});
+        	createBindBridge(host, "open", openBindSource(input, controller, {
+        		onUserChange: (open) => input.onOpenChange?.(open),
+        		destroy: () => {
+        			observer?.disconnect();
+        			controller.destroy();
+        		}
+        	}));
+        	return () => disposeBindBridge(host);
+    });
+
+    effect(() => {
+
+        	getBindBridge(host, "open")?.applyFromSignal();
+    });
+
+    effect.once(({ host, signal }) => {
+        host.addEventListener("animationend", (event) => {
+            const __target = event.target;
+            if (!(__target instanceof Element) || !__target.closest("[data-slot='popover-content']")) return;
+
+            	const root = host.matches("[data-slot=\"popover\"]") ? host : host.querySelector("[data-slot=\"popover\"]");
+            	if (root) syncPopoverArrowSide(root);
+        }, { signal });
+    });
+
+    effect.once(({ host, signal }) => {
+        host.addEventListener("transitionend", (event) => {
+            const __target = event.target;
+            if (!(__target instanceof Element) || !__target.closest("[data-slot='popover-content']")) return;
+
+            	const root = host.matches("[data-slot=\"popover\"]") ? host : host.querySelector("[data-slot=\"popover\"]");
+            	if (root) syncPopoverArrowSide(root);
+        }, { signal });
+    });
+
+    return renderPopover(normalizeStaticChildSlots(input, [
 	"content",
 	"trigger",
 	"children"
-])));
+]));
+});
 function PopoverBase(input = {}) {
 	return renderPopover(normalizeStaticChildSlots(input, [
 		"content",
@@ -4250,42 +4391,54 @@ function renderHoverCard(input = {}) {
 		skipDelayDuration
 	})}${toAttrs(rootProps)}`, inner);
 }
-const HoverCardRoot = ilha.input().action("openChange", (open, { host }) => {
+const HoverCardRoot = ilha((input) => {
+    let host;
+    const openChange = action((open) => {
 	getBindBridge(host, "open")?.onUserChange(open);
-}).onMount(({ host, input, action }) => {
-	const root = host.matches("[data-slot=\"hover-card\"]") ? host : host.querySelector("[data-slot=\"hover-card\"]");
-	if (!root) return;
-	disposeBindBridge(host, "open");
-	stampMorphPreserve(root, MORPH_CONTROLLER_STYLE);
-	const controller = HoverCard$1.createHoverCard(root, {
-		align: input.align,
-		alignOffset: input.alignOffset,
-		avoidCollisions: input.avoidCollisions,
-		closeDelay: input.closeDelay,
-		closeOnClickOutside: input.closeOnClickOutside,
-		closeOnEscape: input.closeOnEscape,
-		collisionPadding: input.collisionPadding,
-		defaultOpen: openBindDefault(input, input.defaultOpen),
-		delay: input.delay,
-		onOpenChange: (open) => action.openChange(open),
-		portal: input.portal,
-		side: input.side,
-		sideOffset: input.sideOffset,
-		skipDelayDuration: input.skipDelayDuration,
-		onPortalMounted: input.onPortalMounted
-	});
-	createBindBridge(host, "open", openBindSource(input, controller, {
-		onUserChange: (open) => input.onOpenChange?.(open),
-		destroy: () => controller.destroy()
-	}));
-	return () => disposeBindBridge(host);
-}).effect(({ host }) => {
-	getBindBridge(host, "open")?.applyFromSignal();
-}).render(({ input }) => renderHoverCard(normalizeStaticChildSlots(input, [
+});
+
+    effect.once(({ host: __host, signal }) => {
+        host = __host;
+
+        	const root = host.matches("[data-slot=\"hover-card\"]") ? host : host.querySelector("[data-slot=\"hover-card\"]");
+        	if (!root) return;
+        	disposeBindBridge(host, "open");
+        	stampMorphPreserve(root, MORPH_CONTROLLER_STYLE);
+        	const controller = HoverCard$1.createHoverCard(root, {
+        		align: input.align,
+        		alignOffset: input.alignOffset,
+        		avoidCollisions: input.avoidCollisions,
+        		closeDelay: input.closeDelay,
+        		closeOnClickOutside: input.closeOnClickOutside,
+        		closeOnEscape: input.closeOnEscape,
+        		collisionPadding: input.collisionPadding,
+        		defaultOpen: openBindDefault(input, input.defaultOpen),
+        		delay: input.delay,
+        		onOpenChange: (open) => openChange(open),
+        		portal: input.portal,
+        		side: input.side,
+        		sideOffset: input.sideOffset,
+        		skipDelayDuration: input.skipDelayDuration,
+        		onPortalMounted: input.onPortalMounted
+        	});
+        	createBindBridge(host, "open", openBindSource(input, controller, {
+        		onUserChange: (open) => input.onOpenChange?.(open),
+        		destroy: () => controller.destroy()
+        	}));
+        	return () => disposeBindBridge(host);
+    });
+
+    effect(() => {
+
+        	getBindBridge(host, "open")?.applyFromSignal();
+    });
+
+    return renderHoverCard(normalizeStaticChildSlots(input, [
 	"content",
 	"trigger",
 	"children"
-])));
+]));
+});
 function HoverCardBase(input = {}) {
 	return renderHoverCard(normalizeStaticChildSlots(input, [
 		"content",
@@ -4363,18 +4516,26 @@ function renderProgress(input = {}) {
 	})}
   </div>`;
 }
-const ProgressRoot = ilha.input().onMount(({ host, input }) => {
-	const root = host.matches("[data-slot=\"progress\"]") ? host : host.querySelector("[data-slot=\"progress\"]");
-	if (!root) return;
-	stampMorphPreserve(root, MORPH_CONTROLLER_STYLE);
-	const controller = Progress$1.createProgress(root, {
-		value: input.value,
-		min: input.min,
-		max: input.max,
-		onValueChange: input.onValueChange
-	});
-	return () => controller.destroy();
-}).render(({ input }) => renderProgress(input));
+const ProgressRoot = ilha((input) => {
+    let host;
+
+    effect.once(({ host: __host, signal }) => {
+        host = __host;
+
+        	const root = host.matches("[data-slot=\"progress\"]") ? host : host.querySelector("[data-slot=\"progress\"]");
+        	if (!root) return;
+        	stampMorphPreserve(root, MORPH_CONTROLLER_STYLE);
+        	const controller = Progress$1.createProgress(root, {
+        		value: input.value,
+        		min: input.min,
+        		max: input.max,
+        		onValueChange: input.onValueChange
+        	});
+        	return () => controller.destroy();
+    });
+
+    return renderProgress(input);
+});
 const Progress = Object.assign(ProgressRoot, {
 	Root: ProgressRoot,
 	Static: renderProgress,
@@ -4464,23 +4625,31 @@ function renderSlider(input = {}) {
     </div>
   </div>`;
 }
-const SliderRoot = ilha.input().onMount(({ host, input }) => {
-	const root = host.matches("[data-slot=\"slider\"]") ? host : host.querySelector("[data-slot=\"slider\"]");
-	if (!root) return;
-	stampMorphPreserve(root, MORPH_CONTROLLER_STYLE);
-	const controller = Slider$1.createSlider(root, {
-		defaultValue: input.value ?? input.defaultValue,
-		min: input.min,
-		max: input.max,
-		step: input.step,
-		orientation: input.orientation,
-		disabled: input.disabled,
-		thumbAlignment: input.thumbAlignment,
-		onValueChange: input.onValueChange,
-		onValueCommit: input.onValueCommit
-	});
-	return () => controller.destroy();
-}).render(({ input }) => renderSlider(input));
+const SliderRoot = ilha((input) => {
+    let host;
+
+    effect.once(({ host: __host, signal }) => {
+        host = __host;
+
+        	const root = host.matches("[data-slot=\"slider\"]") ? host : host.querySelector("[data-slot=\"slider\"]");
+        	if (!root) return;
+        	stampMorphPreserve(root, MORPH_CONTROLLER_STYLE);
+        	const controller = Slider$1.createSlider(root, {
+        		defaultValue: input.value ?? input.defaultValue,
+        		min: input.min,
+        		max: input.max,
+        		step: input.step,
+        		orientation: input.orientation,
+        		disabled: input.disabled,
+        		thumbAlignment: input.thumbAlignment,
+        		onValueChange: input.onValueChange,
+        		onValueCommit: input.onValueCommit
+        	});
+        	return () => controller.destroy();
+    });
+
+    return renderSlider(input);
+});
 const Slider = Object.assign(SliderRoot, {
 	Root: SliderRoot,
 	Static: renderSlider,
@@ -4699,36 +4868,79 @@ function emitPageSizeChange(root, pageSize) {
 function resolvePaginationRoot(host) {
 	return host.matches("[data-slot=\"pagination\"]") ? host : host.querySelector("[data-slot=\"pagination\"]");
 }
-const PaginationRoot = ilha.input().action("pageChange", (page, { input, host }) => {
+const PaginationRoot = ilha((input) => {
+    let host;
+    const pageChange = action((page) => {
 	const root = resolvePaginationRoot(host);
 	const max = maxPage(input.totalCount, input.perPage);
 	const next = clamp(page, 1, max);
 	input.setPage?.(next);
 	input.onPageChange?.(next);
 	if (root) emitPageChange(root, next);
-}).action("pageSizeChange", (size, { input, host }) => {
+});
+    const pageSizeChange = action((size) => {
 	const root = resolvePaginationRoot(host);
 	input.onPageSizeChange?.(size);
 	if (root) emitPageSizeChange(root, size);
-}).on("[data-pagination-action]@click", ({ host, event, action }) => {
-	const root = resolvePaginationRoot(host);
-	const button = event.target?.closest?.("[data-pagination-action]");
-	if (!root || !button || !root.contains(button) || button.hasAttribute("disabled")) return;
-	const page = Number(button.dataset["paginationPage"]);
-	if (Number.isFinite(page)) action.pageChange(page);
-}).on("[data-pagination-page-input]@focusout", ({ event, action }) => {
-	const target = event.target;
-	if (target instanceof HTMLInputElement && target.hasAttribute("data-pagination-page-input")) action.pageChange(Number(target.value));
-}).on("[data-pagination-page-input]@keydown", ({ event, action }) => {
-	const target = event.target;
-	if (event.key === "Enter" && target instanceof HTMLInputElement && target.hasAttribute("data-pagination-page-input")) action.pageChange(Number(target.value));
-}).on("[data-pagination-page-select]@change", ({ event, action }) => {
-	const target = event.target;
-	if (target instanceof HTMLSelectElement && target.hasAttribute("data-pagination-page-select")) action.pageChange(Number(target.value));
-}).on("[data-pagination-page-size]@change", ({ event, action }) => {
-	const target = event.target;
-	if (target instanceof HTMLSelectElement && target.hasAttribute("data-pagination-page-size")) action.pageSizeChange(Number(target.value));
-}).render(({ input }) => renderPagination(input));
+});
+
+    effect.once(({ host: __host }) => { host = __host; });
+
+    effect.once(({ host, signal }) => {
+        host.addEventListener("click", (event) => {
+            const __target = event.target;
+            if (!(__target instanceof Element) || !__target.closest("[data-pagination-action]")) return;
+
+            	const root = resolvePaginationRoot(host);
+            	const button = event.target?.closest?.("[data-pagination-action]");
+            	if (!root || !button || !root.contains(button) || button.hasAttribute("disabled")) return;
+            	const page = Number(button.dataset["paginationPage"]);
+            	if (Number.isFinite(page)) pageChange(page);
+        }, { signal });
+    });
+
+    effect.once(({ host, signal }) => {
+        host.addEventListener("focusout", (event) => {
+            const __target = event.target;
+            if (!(__target instanceof Element) || !__target.closest("[data-pagination-page-input]")) return;
+
+            	const target = event.target;
+            	if (target instanceof HTMLInputElement && target.hasAttribute("data-pagination-page-input")) pageChange(Number(target.value));
+        }, { signal });
+    });
+
+    effect.once(({ host, signal }) => {
+        host.addEventListener("keydown", (event) => {
+            const __target = event.target;
+            if (!(__target instanceof Element) || !__target.closest("[data-pagination-page-input]")) return;
+
+            	const target = event.target;
+            	if (event.key === "Enter" && target instanceof HTMLInputElement && target.hasAttribute("data-pagination-page-input")) pageChange(Number(target.value));
+        }, { signal });
+    });
+
+    effect.once(({ host, signal }) => {
+        host.addEventListener("change", (event) => {
+            const __target = event.target;
+            if (!(__target instanceof Element) || !__target.closest("[data-pagination-page-select]")) return;
+
+            	const target = event.target;
+            	if (target instanceof HTMLSelectElement && target.hasAttribute("data-pagination-page-select")) pageChange(Number(target.value));
+        }, { signal });
+    });
+
+    effect.once(({ host, signal }) => {
+        host.addEventListener("change", (event) => {
+            const __target = event.target;
+            if (!(__target instanceof Element) || !__target.closest("[data-pagination-page-size]")) return;
+
+            	const target = event.target;
+            	if (target instanceof HTMLSelectElement && target.hasAttribute("data-pagination-page-size")) pageSizeChange(Number(target.value));
+        }, { signal });
+    });
+
+    return renderPagination(input);
+});
 function PaginationBase(input = {}) {
 	return renderPagination(input);
 }
@@ -4920,32 +5132,44 @@ function emitSwitchChange(root, checked) {
 function resolveSwitchRoot(host) {
 	return host.matches("[data-slot=\"switch\"]") ? host : host.querySelector("[data-slot=\"switch\"]");
 }
-const SwitchRoot = ilha.input().action("checkedChange", (checked, { host }) => {
+const SwitchRoot = ilha((input) => {
+    let host;
+    const checkedChange = action((checked) => {
 	getBindBridge(host, "checked")?.onUserChange(checked);
 	emitSwitchChange(resolveSwitchRoot(host) ?? host, checked);
-}).onMount(({ host, input, action }) => {
-	const root = resolveSwitchRoot(host);
-	if (!root) return;
-	disposeBindBridge(host, "checked");
-	stampMorphPreserve(root);
-	const controller = Switch$1.createSwitch(root, {
-		defaultChecked: typeof input.checked === "boolean" ? input.checked : typeof input.defaultChecked === "boolean" ? input.defaultChecked : void 0,
-		disabled: typeof input.disabled === "boolean" ? input.disabled : void 0,
-		readOnly: typeof input.readOnly === "boolean" ? input.readOnly : void 0,
-		required: typeof input.required === "boolean" ? input.required : void 0,
-		name: typeof input.name === "string" ? input.name : void 0,
-		value: typeof input.value === "string" ? input.value : void 0,
-		uncheckedValue: typeof input.uncheckedValue === "string" ? input.uncheckedValue : void 0,
-		onCheckedChange: (checked) => action.checkedChange(checked)
-	});
-	createBindBridge(host, "checked", checkedBindSource(input, controller, {
-		onUserChange: (checked) => input.onCheckedChange?.(checked),
-		destroy: () => controller.destroy()
-	}));
-	return () => disposeBindBridge(host);
-}).effect(({ host }) => {
-	getBindBridge(host, "checked")?.applyFromSignal();
-}).render(({ input }) => renderSwitch(input));
+});
+
+    effect.once(({ host: __host, signal }) => {
+        host = __host;
+
+        	const root = resolveSwitchRoot(host);
+        	if (!root) return;
+        	disposeBindBridge(host, "checked");
+        	stampMorphPreserve(root);
+        	const controller = Switch$1.createSwitch(root, {
+        		defaultChecked: typeof input.checked === "boolean" ? input.checked : typeof input.defaultChecked === "boolean" ? input.defaultChecked : void 0,
+        		disabled: typeof input.disabled === "boolean" ? input.disabled : void 0,
+        		readOnly: typeof input.readOnly === "boolean" ? input.readOnly : void 0,
+        		required: typeof input.required === "boolean" ? input.required : void 0,
+        		name: typeof input.name === "string" ? input.name : void 0,
+        		value: typeof input.value === "string" ? input.value : void 0,
+        		uncheckedValue: typeof input.uncheckedValue === "string" ? input.uncheckedValue : void 0,
+        		onCheckedChange: (checked) => checkedChange(checked)
+        	});
+        	createBindBridge(host, "checked", checkedBindSource(input, controller, {
+        		onUserChange: (checked) => input.onCheckedChange?.(checked),
+        		destroy: () => controller.destroy()
+        	}));
+        	return () => disposeBindBridge(host);
+    });
+
+    effect(() => {
+
+        	getBindBridge(host, "checked")?.applyFromSignal();
+    });
+
+    return renderSwitch(input);
+});
 const Switch = Object.assign(SwitchRoot, {
 	Root: SwitchRoot,
 	Static: renderSwitch,
@@ -5403,43 +5627,55 @@ function enhanceOverflow(root) {
 function resolveTabsRoot(host) {
 	return host.matches("[data-slot=\"tabs\"]") ? host : host.querySelector("[data-slot=\"tabs\"]");
 }
-const TabsRoot = ilha.input().action("valueChange", (value, { host }) => {
+const TabsRoot = ilha((input) => {
+    let host;
+    const valueChange = action((value) => {
 	getBindBridge(host, "value")?.onUserChange(value);
-}).onMount(({ host, input, action }) => {
-	const root = resolveTabsRoot(host);
-	if (!root) return;
-	disposeBindBridge(host, "value");
-	const initialValue = groupBindDefault(input, input.value ?? input.selectedValue ?? input.defaultValue) ?? void 0;
-	const defaultValue = typeof initialValue === "string" ? initialValue : Array.isArray(initialValue) ? initialValue[0] : void 0;
-	stampMorphPreserve(root, MORPH_CONTROLLER_STYLE);
-	const controller = Tabs$1.createTabs(root, {
-		defaultValue,
-		activationMode: input.activationMode ?? (input.activateOnFocus ? "auto" : "manual"),
-		onValueChange: (value) => action.valueChange(value)
-	});
-	const cleanupOverflow = enhanceOverflow(root);
-	createBindBridge(host, "value", groupBindSource(input, {
-		getValue: () => controller.value,
-		setValue: (value) => {
-			if (typeof value === "string") controller.select(value);
-			else if (Array.isArray(value) && value[0]) controller.select(value[0]);
-		}
-	}, {
-		onUserChange: (value) => {
-			if (typeof value === "string") input.onValueChange?.(value);
-		},
-		afterControllerWrite: () => controller.updateIndicator(),
-		destroy: () => {
-			cleanupOverflow();
-			controller.destroy();
-		}
-	}));
-	return () => disposeBindBridge(host);
-}).effect(({ host }) => {
-	const root = resolveTabsRoot(host);
-	if (root) stampMorphPreserve(root, MORPH_CONTROLLER_STYLE);
-	getBindBridge(host, "value")?.applyFromSignal();
-}).render(({ input }) => renderTabs(input));
+});
+
+    effect.once(({ host: __host, signal }) => {
+        host = __host;
+
+        	const root = resolveTabsRoot(host);
+        	if (!root) return;
+        	disposeBindBridge(host, "value");
+        	const initialValue = groupBindDefault(input, input.value ?? input.selectedValue ?? input.defaultValue) ?? void 0;
+        	const defaultValue = typeof initialValue === "string" ? initialValue : Array.isArray(initialValue) ? initialValue[0] : void 0;
+        	stampMorphPreserve(root, MORPH_CONTROLLER_STYLE);
+        	const controller = Tabs$1.createTabs(root, {
+        		defaultValue,
+        		activationMode: input.activationMode ?? (input.activateOnFocus ? "auto" : "manual"),
+        		onValueChange: (value) => valueChange(value)
+        	});
+        	const cleanupOverflow = enhanceOverflow(root);
+        	createBindBridge(host, "value", groupBindSource(input, {
+        		getValue: () => controller.value,
+        		setValue: (value) => {
+        			if (typeof value === "string") controller.select(value);
+        			else if (Array.isArray(value) && value[0]) controller.select(value[0]);
+        		}
+        	}, {
+        		onUserChange: (value) => {
+        			if (typeof value === "string") input.onValueChange?.(value);
+        		},
+        		afterControllerWrite: () => controller.updateIndicator(),
+        		destroy: () => {
+        			cleanupOverflow();
+        			controller.destroy();
+        		}
+        	}));
+        	return () => disposeBindBridge(host);
+    });
+
+    effect(() => {
+
+        	const root = resolveTabsRoot(host);
+        	if (root) stampMorphPreserve(root, MORPH_CONTROLLER_STYLE);
+        	getBindBridge(host, "value")?.applyFromSignal();
+    });
+
+    return renderTabs(input);
+});
 const Tabs = Object.assign(TabsRoot, {
 	Root: TabsRoot,
 	Static: renderTabs,
@@ -5587,17 +5823,25 @@ function renderToggle(input = {}) {
     ${render(children)}
   </button>`;
 }
-const ToggleRoot = ilha.input().onMount(({ host, input }) => {
-	const root = host.matches("[data-slot=\"toggle\"]") ? host : host.querySelector("[data-slot=\"toggle\"]");
-	if (!root) return;
-	stampMorphPreserve(root);
-	const controller = Toggle$1.createToggle(root, {
-		defaultPressed: input.defaultPressed,
-		disabled: input.disabled,
-		onPressedChange: input.onPressedChange
-	});
-	return () => controller.destroy();
-}).render(({ input }) => renderToggle(input));
+const ToggleRoot = ilha((input) => {
+    let host;
+
+    effect.once(({ host: __host, signal }) => {
+        host = __host;
+
+        	const root = host.matches("[data-slot=\"toggle\"]") ? host : host.querySelector("[data-slot=\"toggle\"]");
+        	if (!root) return;
+        	stampMorphPreserve(root);
+        	const controller = Toggle$1.createToggle(root, {
+        		defaultPressed: input.defaultPressed,
+        		disabled: input.disabled,
+        		onPressedChange: input.onPressedChange
+        	});
+        	return () => controller.destroy();
+    });
+
+    return renderToggle(input);
+});
 const Toggle = Object.assign(ToggleRoot, {
 	Root: ToggleRoot,
 	Static: renderToggle
@@ -5642,36 +5886,48 @@ function renderToggleGroup(input = {}) {
 function resolveToggleGroupRoot(host) {
 	return host.matches("[data-slot=\"toggle-group\"]") ? host : host.querySelector("[data-slot=\"toggle-group\"]");
 }
-const ToggleGroupRoot = ilha.input().action("valueChange", (value, { host }) => {
+const ToggleGroupRoot = ilha((input) => {
+    let host;
+    const valueChange = action((value) => {
 	getBindBridge(host, "value")?.onUserChange(value);
-}).onMount(({ host, input, action }) => {
-	const root = resolveToggleGroupRoot(host);
-	if (!root) return;
-	const mode = input.type === "multiple" ? "multiple" : "single";
-	disposeBindBridge(host, "value");
-	stampMorphPreserve(root);
-	const controller = ToggleGroup$1.createToggleGroup(root, {
-		defaultValue: groupBindDefault(input, input.defaultValue) ?? void 0,
-		multiple: input.type === "multiple",
-		orientation: input.orientation,
-		loop: input.loop,
-		disabled: input.disabled,
-		onValueChange: (value) => action.valueChange(value)
-	});
-	createBindBridge(host, "value", groupBindSource(input, {
-		getValue: () => controller.value,
-		setValue: (value) => controller.setValue(value ?? [])
-	}, {
-		mode,
-		onUserChange: (value) => {
-			if (Array.isArray(value)) input.onValueChange?.(value);
-		},
-		destroy: () => controller.destroy()
-	}));
-	return () => disposeBindBridge(host);
-}).effect(({ host }) => {
-	getBindBridge(host, "value")?.applyFromSignal();
-}).render(({ input }) => renderToggleGroup(input));
+});
+
+    effect.once(({ host: __host, signal }) => {
+        host = __host;
+
+        	const root = resolveToggleGroupRoot(host);
+        	if (!root) return;
+        	const mode = input.type === "multiple" ? "multiple" : "single";
+        	disposeBindBridge(host, "value");
+        	stampMorphPreserve(root);
+        	const controller = ToggleGroup$1.createToggleGroup(root, {
+        		defaultValue: groupBindDefault(input, input.defaultValue) ?? void 0,
+        		multiple: input.type === "multiple",
+        		orientation: input.orientation,
+        		loop: input.loop,
+        		disabled: input.disabled,
+        		onValueChange: (value) => valueChange(value)
+        	});
+        	createBindBridge(host, "value", groupBindSource(input, {
+        		getValue: () => controller.value,
+        		setValue: (value) => controller.setValue(value ?? [])
+        	}, {
+        		mode,
+        		onUserChange: (value) => {
+        			if (Array.isArray(value)) input.onValueChange?.(value);
+        		},
+        		destroy: () => controller.destroy()
+        	}));
+        	return () => disposeBindBridge(host);
+    });
+
+    effect(() => {
+
+        	getBindBridge(host, "value")?.applyFromSignal();
+    });
+
+    return renderToggleGroup(input);
+});
 function ToggleGroupSeparator(input = {}) {
 	const { class: className, className: aliasedClassName, ...rest } = input;
 	return html`<div
@@ -5900,7 +6156,20 @@ function renderResizable(input = {}) {
     ${renderChildren(normalizeResizableChildren(children))}
   </div>`;
 }
-const ResizablePanelGroupRoot = ilha.input().onMount(({ host, input }) => scheduleConnectResizableTree(host, input)).effect(({ host, input }) => scheduleConnectResizableTree(host, input)).render(({ input }) => renderResizable(input));
+const ResizablePanelGroupRoot = ilha((input) => {
+    let host;
+
+    effect.once(({ host: __host, signal }) => {
+        host = __host;
+        return scheduleConnectResizableTree(host, input);
+    });
+
+    effect(() => {
+        scheduleConnectResizableTree(host, input)
+    });
+
+    return renderResizable(input);
+});
 function autoMountResizable(scope = document) {
 	scheduleConnectResizableTree(scope);
 }
diff --git a/dist/sonner.js b/dist/sonner.js
index b37feb9..e2b1450 100644
--- a/dist/sonner.js
+++ b/dist/sonner.js
@@ -1,5 +1,5 @@
 import { n as cn, t as toAttrs } from "./input-3v9lVkrk.js";
-import { html, ilha, raw } from "ilha";
+import { html, ilha, raw, effect } from "ilha";
 import { toast as toast$1 } from "sonner";
 //#region src/components/sonner/index.ts
 const toast = Object.assign((message, data) => {
@@ -493,11 +493,19 @@ if (typeof document !== "undefined") {
 	if (document.readyState === "loading") document.addEventListener("DOMContentLoaded", boot, { once: true });
 	else queueMicrotask(boot);
 }
-const ToasterRoot = ilha.input().onMount(({ host, input }) => {
-	const root = host.matches("[data-areia-sonner-toaster]") ? host : host.querySelector("[data-areia-sonner-toaster]");
-	if (!root) return;
-	return acquireDocumentRuntime(root.ownerDocument).registerOwner(root, input);
-}).render(({ input }) => renderToaster(input));
+const ToasterRoot = ilha((input) => {
+    let host;
+
+    effect.once(({ host: __host, signal }) => {
+        host = __host;
+
+        	const root = host.matches("[data-areia-sonner-toaster]") ? host : host.querySelector("[data-areia-sonner-toaster]");
+        	if (!root) return;
+        	return acquireDocumentRuntime(root.ownerDocument).registerOwner(root, input);
+    });
+
+    return renderToaster(input);
+});
 function ToasterStatic(input = {}) {
 	return renderToaster(input);
 }
