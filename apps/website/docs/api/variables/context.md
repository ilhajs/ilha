# Variable: context()

> `const` **context**: \<`T`\>(`key`, `initial`) => `ContextSignal`\<`T`\> = `ilhaContext`

## Type Parameters

### T

`T`

## Parameters

### key

`string`

### initial

`T`

## Returns

`ContextSignal`\<`T`\>
