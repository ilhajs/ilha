---
title: "Tutorial: State"
description: Tutorial — define reactive state primitives that trigger UI updates with the state() function.
seo:
  title: Add counter state with state() - Ilha
---

import { example } from "./state.examples";

Ilha uses signals to make components reactive. Use the `state()` primitive to define state — any value you declare here automatically triggers a UI update when it changes. The component returns JSX directly, and the state accessor is read inside it.

<Preview code={example} size="lg" />

## Similar concepts

- React: `useState`
- Vue: `reactive()`
- Svelte: `$state()`
