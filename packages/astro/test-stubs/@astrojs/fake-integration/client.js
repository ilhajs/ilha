// Client (hydration) entrypoint for the `@astrojs/solid-js` stub renderer.
// A no-op — the fixture only asserts SSR routing, never runs client hydration.
const hydrate = () => {
  /* empty */
};
export { hydrate };
export default hydrate;
